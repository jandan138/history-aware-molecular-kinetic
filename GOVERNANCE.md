# Governance

The project is currently maintainer-led. Architecture decisions that alter
benchmark semantics, artifact schemas, scientific claims, or license boundaries
must be recorded as Architecture Decision Records under `docs/decisions/`.

Frozen benchmark cases are immutable. Corrections create a new case revision
with a migration note; they never silently replace published evidence.
