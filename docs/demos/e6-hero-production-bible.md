# E6 Hero production bible — Black Obsidian Time Laboratory

## Logline

> Same visible present. Different hidden velocity ownership. A chosen physical
> future.

The primary Hero is E5's frozen Same Present, Chosen Future recipe. It is not an
anthology of all project experiments.

## Scientific lock

| Field | Frozen value |
|---|---|
| study | `MOLECULAR-TIME-MACHINE-E5-v0` |
| source world | exact-reverse `N=256`, seed 4 |
| pivot | `t=0.80` |
| target | Molecular E middle-stroke extension |
| legal palette | 30 complete-EDMD previews |
| selected surgery | `(48,82)` and `(174,211)` |
| touched particles | `4/256` |
| target reduction | `75%`, occupancy `8→2` |
| collateral retention | `100%`, `19/19` |

Allowed short wording: **same visible present**.

Required precise wording in captions: **same declared `4×2` resolved present, not
the same exact microstate**.

## Visual system

- **World:** black obsidian stage with a thin transparent molecular chamber.
- **Physical plane:** the original `(x,y)` plane maps to world `(x,y,radius)`.
  Particle centres receive no decorative depth offset.
- **Passive particles:** graphite and pearl, low saturation, common roughness.
- **Foreground glyph:** warm ivory.
- **Selected future target:** cyan.
- **Collision history:** amber.
- **Velocity-ownership surgery:** coral.
- **Branches:** panel borders, labels, or ghost trails; never particle recoloring.
- **Typography:** minimal open-source sans serif, title case, no debug tables in the
  Hero master.

The comparison camera, particle radius, passive materials, lights, frame times,
tone mapping, sampling, motion treatment, and output transform are locked between
branches.

## Forty-five-second master

| Time | Shot | Primary question |
|---:|---|---|
| `0–5s` | The particles recover Molecular E inside the chamber. | What future is about to appear? |
| `5–10s` | The creator selects the E middle-stroke extension. | What should change? |
| `10–16s` | Time returns to `t=0.80`; the glyph is hidden and both branches overlap. | Can the visible present stay fixed? |
| `16–23s` | Thirty legal previews form a restrained future fan. | Which hidden edit produces the target? |
| `23–29s` | Four particles hold position while two pairs exchange velocity cores. | What was edited? |
| `29–35s` | A locked camera confirms identical particle positions, then the branches split. | Is this a real branch from one visible present? |
| `35–42s` | Original E and chosen C-like futures resolve side by side. | Did the chosen future happen? |
| `42–45s` | `4/256`, `75%`, `100%`, then the title. | What is the memorable result? |

## Technical video and companion

The 3–5 minute technical video retains the full five-act paper order. E2 receives
one short transition: the dose ladder moves systematically, but its wiring claim
fails, motivating causal editing rather than another slider.

The real-time companion has three rooms:

1. **Reveal** — E1 pivot and two futures;
2. **Trace & Branch** — E3/E4 event selection, ranking, and cached exact previews;
3. **Author** — E5 target, future palette, selected surgery, and E/C-like playback.

The companion never runs physics in the browser. It is a renderer and interaction
client for frozen exact results.

## Output contract

- 4K, 24 fps, 45-second teaser;
- multilayer half-float EXR frame archive;
- ProRes master and H.264 delivery encode;
- 4K representative stills for pivot, swap, and payoff;
- 3–5 minute technical video;
- self-contained static web companion;
- per-output renderer, camera, source-artifact, metric, and content hashes.
